{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "lerna run watch --stream",
    "build": "lerna run build --concurrency=4",
    "dev": "lerna run dev:start --stream"
  },
  "devDependencies": {
    "@types/node": "^22.9.1",
    "lerna": "^8.1.9",
    "typescript": "^5.6.3"
  },
  "dependencies": {}
}
